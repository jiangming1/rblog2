<%= form_for @yuehui, :html => { :class => "form-horizontal yuehui" } do |f| %>

    <% if @yuehui.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@yuehui.errors.count, "error") %> prohibited this yuehui from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @yuehui.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="control-group">
    <%= f.label :xingming, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :xingming, :class => 'form-control' %>
    </div>
    <%= error_span(@yuehui[:xingming]) %>
  </div>
  <div class="control-group">
    <%= f.label :tel, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :tel, :class => 'form-control' %>
    </div>
    <%= error_span(@yuehui[:tel]) %>
  </div>
  <div class="control-group">
    <%= f.label :addr, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :addr, :class => 'form-control' %>
    </div>
    <%= error_span(@yuehui[:addr]) %>
  </div>


  <%= f.submit nil, :class => 'btn btn-primary' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
            yuehuis_path, :class => 'btn btn-default' %>

<% end %>
