<%= form_for(@smsuserusedtellist) do |f| %>
  <% if @smsuserusedtellist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@smsuserusedtellist.errors.count, "error") %> prohibited this smsuserusedtellist from being saved:</h2>

      <ul>
      <% @smsuserusedtellist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :telpostion %><br>
    <%= f.number_field :telpostion %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
