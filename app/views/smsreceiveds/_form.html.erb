<%= form_for(@smsreceived) do |f| %>
  <% if @smsreceived.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@smsreceived.errors.count, "error") %> prohibited this smsreceived from being saved:</h2>

      <ul>
      <% @smsreceived.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :no %><br>
    <%= f.text_field :no %>
  </div>
  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_field :body %>
  </div>
  <div class="field">
    <%= f.label :tag %><br>
    <%= f.text_field :tag %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
